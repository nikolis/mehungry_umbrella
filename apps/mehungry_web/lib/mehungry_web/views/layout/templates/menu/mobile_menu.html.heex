<% show_invitations =
  @current_user && !Mehungry.Subscriptions.nutritionist?(@current_user.id) &&
    Mehungry.Professionals.count_pending_invitations_for_client(@current_user.id) > 0 %>
<nav
  class="nav_container_mobile fixed border-t border-ink-panel2 bg-ink-panel text-parchment-dim"
  id="nav_bar_mobile"
  phx-update="ignore"
>
  <div
    class={"grid #{if show_invitations, do: "grid-cols-8", else: "grid-cols-7"} bg-inherit"}
    style="overflow: hidden; max-height: 64px;"
  >
    <!-- Logo -->
    <.link
      class="flex items-center justify-center p-3 nav_link_basic hover:text-parchment transition-colors"
      patch="/"
    >
      {MehungryWeb.SvgComponents.logo_mark(%{id: "nav-mobile", class: "h-7 w-7 text-white"})}
    </.link>

    <!-- Browse -->
    <.link
      patch="/browse"
      class="flex items-center justify-center p-3 nav_link_basic hover:text-parchment transition-colors"
    >
      <.icon name="hero-book-open" class="h-6 w-6" />
    </.link>

    <a
      href="/conditions"
      class="nav_link_basic flex items-center gap-3 px-3 py-2.5 rounded-lg text-parchment-dim hover:text-parchment hover:bg-ink-panel2 transition-colors"
    >
      <.icon
        name="hero-heart"
        class="w-5 h-5 flex-shrink-0"
      />
    </a>

    <!-- Foods -->
    <.link
      patch="/foods"
      class="flex items-center justify-center p-3 nav_link_basic hover:text-parchment transition-colors"
    >
      <svg
        class="h-6 w-6"
        fill="none"
        stroke="currentColor"
        stroke-width="2"
        viewBox="0 0 24 24"
        xmlns="http://www.w3.org/2000/svg"
      >
        <path
          stroke-linecap="round"
          stroke-linejoin="round"
          d="M4 6h16a1 1 0 0 1 1 1v3a8 8 0 0 1-8 8 8 8 0 0 1-8-8V7a1 1 0 0 1 1-1z"
        />
      </svg>
    </.link>

    <!-- Create -->
    <.link
      patch="/create_recipe"
      class="flex items-center justify-center p-3 nav_link_basic hover:text-parchment transition-colors"
    >
      <.icon name="hero-plus-circle" class="h-6 w-6" />
    </.link>

    <!-- Calendar -->
    <.link
      patch="/calendar"
      class="flex items-center justify-center p-3 nav_link_basic hover:text-parchment transition-colors"
    >
      <.icon name="hero-calendar" class="h-6 w-6" />
    </.link>

    <!-- Basket -->
    <.link
      patch="/basket"
      class="flex items-center justify-center p-3 nav_link_basic hover:text-parchment transition-colors"
    >
      <.icon name="hero-shopping-cart" class="h-6 w-6" />
    </.link>

    <!-- Tutor invitations (non-professional users with pending invitations) -->
    <%= if show_invitations do %>
      <a
        href="/notifications/invitations"
        class="flex items-center justify-center p-3 nav_link_basic hover:text-parchment transition-colors"
      >
        <div class="relative">
          <.icon name="hero-bell" class="h-6 w-6" />
          <span class="absolute -top-1 -right-1 w-4 h-4 bg-red-500 rounded-full text-[10px] text-white font-bold flex items-center justify-center">
            {Mehungry.Professionals.count_pending_invitations_for_client(@current_user.id)}
          </span>
        </div>
      </a>
    <% end %>

    <!-- Profile / Login -->
    <a
      href={~p"/profile"}
      class="flex items-center justify-center p-3 nav_link_basic hover:text-parchment transition-colors"
    >
      <%= if @current_user do %>
        <img
          src={@current_user.profile_pic}
          class="w-6 h-6 rounded-full object-cover border border-ink-panel2"
        />
      <% else %>
        <.icon name="hero-arrow-right-on-rectangle" class="h-6 w-6" />
      <% end %>
    </a>
  </div>
</nav>
